<h2><%=l(:label_resolutions_new)%></h2>

<%= labelled_form_for @issue_resolution, :as => :issue_resolution,
                      :url => project_issue_resolutions_path(@project) do |f| %>
<%= render :partial => 'issue_resolutions/form', :locals => { :f => f } %>
<%= submit_tag l(:button_create) %>
<% end %>
