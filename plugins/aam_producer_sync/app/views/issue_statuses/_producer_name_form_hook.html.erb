<%  producer_ticket_statuses = #Producer API will only accept these
  [ 'open', 'in_progress', 'on_hold', 'resolved', 'closed', 'rejected' ] %>
<p>
<%= form.select :name_raw, producer_ticket_statuses, {:required => true, :include_blank => true} %>
</p>